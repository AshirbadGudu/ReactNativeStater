import React from 'react';
import {View} from 'react-native';
import Topbar from '../components/Topbar';

const Home = (props) => {
  return (
    <View>
      <Topbar title={'Home'} />
    </View>
  );
};

export default Home;
